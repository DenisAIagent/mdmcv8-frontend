<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Mes SmartLinks | MDMC Dashboard</title>
  <link rel="icon" href="data:image/svg+xml;base64," type="image/svg+xml">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
  
  <style>
    :root {
      --primary: #E50914;
      --primary-hover: #cc271a;
      --primary-light: rgba(229, 9, 20, 0.1);
      --background: #0a0a0a;
      --surface: #111111;
      --surface-light: #161616;
      --surface-hover: #1a1a1a;
      --border: rgba(255, 255, 255, 0.08);
      --border-hover: rgba(255, 255, 255, 0.16);
      --text-primary: #ffffff;
      --text-secondary: #a0a0a0;
      --text-muted: #666666;
      --success: #10b981;
      --warning: #f59e0b;
      --error: #ef4444;
      --info: #3b82f6;
      --gradient: linear-gradient(135deg, var(--primary) 0%, #cc271a 100%);
      --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.3), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
      --shadow-lg: 0 20px 25px -5px rgba(0, 0, 0, 0.3), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
      --radius: 12px;
      --radius-sm: 8px;
      --transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', sans-serif;
      background: var(--background);
      color: var(--text-primary);
      line-height: 1.6;
      font-size: 14px;
    }

    /* Header avec navigation */
    .header {
      background: var(--surface);
      border-bottom: 1px solid var(--border);
      padding: 16px 24px;
      position: sticky;
      top: 0;
      z-index: 100;
      backdrop-filter: blur(10px);
    }

    .header-content {
      max-width: 1400px;
      margin: 0 auto;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .header-left {
      display: flex;
      align-items: center;
      gap: 24px;
    }

    .logo {
      height: 32px;
      width: auto;
    }

    .nav-tabs {
      display: flex;
      gap: 4px;
    }

    .nav-tab {
      padding: 6px 12px;
      border-radius: var(--radius-sm);
      color: var(--text-secondary);
      text-decoration: none;
      font-size: 13px;
      font-weight: 500;
      transition: var(--transition);
    }

    .nav-tab:hover {
      color: var(--text-primary);
      background: var(--surface-light);
    }

    .nav-tab.active {
      color: var(--primary);
      background: var(--primary-light);
    }

    .header-right {
      display: flex;
      align-items: center;
      gap: 16px;
    }

    .user-info {
      font-size: 13px;
      color: var(--text-secondary);
    }

    .logout-btn {
      background: none;
      border: 1px solid var(--border);
      color: var(--text-secondary);
      padding: 6px 12px;
      border-radius: var(--radius-sm);
      font-size: 12px;
      cursor: pointer;
      transition: var(--transition);
      font-weight: 500;
    }

    .logout-btn:hover {
      border-color: var(--primary);
      color: var(--primary);
    }

    /* Container principal */
    .container {
      max-width: 1400px;
      margin: 0 auto;
      padding: 32px 24px;
    }

    /* Header de page */
    .page-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 24px;
    }

    .page-title-section h1 {
      font-size: 28px;
      font-weight: 700;
      color: var(--text-primary);
      margin-bottom: 4px;
      letter-spacing: -0.025em;
    }

    .page-subtitle {
      color: var(--text-secondary);
      font-size: 14px;
    }

    .page-actions {
      display: flex;
      gap: 12px;
    }

    /* Statistiques en haut */
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 16px;
      margin-bottom: 24px;
    }

    .stat-card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 16px;
      transition: var(--transition);
    }

    .stat-card:hover {
      border-color: var(--border-hover);
    }

    .stat-value {
      font-size: 24px;
      font-weight: 700;
      color: var(--text-primary);
      margin-bottom: 4px;
    }

    .stat-label {
      font-size: 12px;
      color: var(--text-secondary);
      text-transform: uppercase;
      font-weight: 500;
      letter-spacing: 0.5px;
    }

    .stat-change {
      font-size: 11px;
      margin-top: 4px;
      display: flex;
      align-items: center;
      gap: 4px;
    }

    .stat-change.positive {
      color: var(--success);
    }

    .stat-change.negative {
      color: var(--error);
    }

    /* Contrôles et filtres */
    .controls {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 16px;
      margin-bottom: 24px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 16px;
    }

    .controls-left {
      display: flex;
      align-items: center;
      gap: 12px;
      flex: 1;
    }

    .search-input {
      background: var(--surface-light);
      border: 1px solid var(--border);
      border-radius: var(--radius-sm);
      padding: 8px 12px;
      color: var(--text-primary);
      font-size: 13px;
      width: 280px;
      transition: var(--transition);
    }

    .search-input:focus {
      outline: none;
      border-color: var(--primary);
      box-shadow: 0 0 0 3px var(--primary-light);
    }

    .search-input::placeholder {
      color: var(--text-muted);
    }

    .filter-select {
      background: var(--surface-light);
      border: 1px solid var(--border);
      border-radius: var(--radius-sm);
      padding: 8px 12px;
      color: var(--text-primary);
      font-size: 13px;
      cursor: pointer;
      transition: var(--transition);
    }

    .filter-select:focus {
      outline: none;
      border-color: var(--primary);
    }

    .controls-right {
      display: flex;
      gap: 8px;
    }

    /* Boutons */
    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 6px;
      padding: 8px 12px;
      border-radius: var(--radius-sm);
      font-size: 13px;
      font-weight: 500;
      cursor: pointer;
      transition: var(--transition);
      border: none;
      text-decoration: none;
      font-family: inherit;
      white-space: nowrap;
    }

    .btn-primary {
      background: var(--gradient);
      color: white;
      box-shadow: var(--shadow);
    }

    .btn-primary:hover {
      transform: translateY(-1px);
      box-shadow: var(--shadow-lg);
    }

    .btn-secondary {
      background: var(--surface-light);
      color: var(--text-secondary);
      border: 1px solid var(--border);
    }

    .btn-secondary:hover {
      border-color: var(--border-hover);
      color: var(--text-primary);
    }

    .btn-ghost {
      background: none;
      color: var(--text-secondary);
      border: 1px solid transparent;
    }

    .btn-ghost:hover {
      color: var(--text-primary);
      background: var(--surface-light);
    }

    .btn-sm {
      padding: 4px 8px;
      font-size: 12px;
    }

    /* Liste des SmartLinks */
    .smartlinks-list {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      overflow: hidden;
    }

    .smartlink-item {
      display: grid;
      grid-template-columns: 60px 1fr auto auto;
      align-items: center;
      padding: 16px;
      border-bottom: 1px solid var(--border);
      transition: var(--transition);
      gap: 16px;
    }

    .smartlink-item:last-child {
      border-bottom: none;
    }

    .smartlink-item:hover {
      background: var(--surface-hover);
    }

    /* Artwork */
    .smartlink-artwork {
      width: 48px;
      height: 48px;
      border-radius: var(--radius-sm);
      object-fit: cover;
      background: var(--surface-light);
      border: 1px solid var(--border);
    }

    /* Info principale */
    .smartlink-info {
      display: flex;
      flex-direction: column;
      gap: 4px;
      min-width: 0;
    }

    .smartlink-title {
      font-weight: 600;
      color: var(--text-primary);
      font-size: 14px;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    .smartlink-artist {
      color: var(--text-secondary);
      font-size: 12px;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    .smartlink-meta {
      display: flex;
      align-items: center;
      gap: 12px;
      font-size: 11px;
      color: var(--text-muted);
      margin-top: 2px;
    }

    .smartlink-platforms {
      display: flex;
      align-items: center;
      gap: 4px;
    }

    .platform-count {
      background: var(--surface-light);
      padding: 2px 6px;
      border-radius: 4px;
      font-size: 10px;
      font-weight: 500;
      color: var(--text-secondary);
    }

    /* Statistiques inline */
    .smartlink-stats {
      display: flex;
      align-items: center;
      gap: 16px;
      font-size: 12px;
    }

    .stat-item {
      text-align: center;
      min-width: 50px;
    }

    .stat-number {
      font-weight: 600;
      color: var(--text-primary);
      display: block;
    }

    .stat-text {
      color: var(--text-muted);
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    /* Actions */
    .smartlink-actions {
      display: flex;
      align-items: center;
      gap: 4px;
      opacity: 0;
      transition: var(--transition);
    }

    .smartlink-item:hover .smartlink-actions {
      opacity: 1;
    }

    .action-btn {
      width: 32px;
      height: 32px;
      display: flex;
      align-items: center;
      justify-content: center;
      border-radius: var(--radius-sm);
      background: none;
      border: 1px solid transparent;
      color: var(--text-muted);
      cursor: pointer;
      transition: var(--transition);
      font-size: 14px;
    }

    .action-btn:hover {
      color: var(--text-primary);
      background: var(--surface-light);
      border-color: var(--border);
    }

    .action-btn.danger:hover {
      color: var(--error);
      background: rgba(239, 68, 68, 0.1);
      border-color: var(--error);
    }

    .action-btn.primary:hover {
      color: var(--primary);
      background: var(--primary-light);
      border-color: var(--primary);
    }

    /* États vides */
    .empty-state {
      text-align: center;
      padding: 48px 24px;
      color: var(--text-muted);
    }

    .empty-state h3 {
      font-size: 18px;
      color: var(--text-secondary);
      margin-bottom: 8px;
    }

    .empty-state p {
      font-size: 14px;
      margin-bottom: 24px;
    }

    /* Modal */
    .modal-overlay {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(0, 0, 0, 0.8);
      backdrop-filter: blur(8px);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 1000;
      opacity: 0;
      visibility: hidden;
      transition: var(--transition);
    }

    .modal-overlay.visible {
      opacity: 1;
      visibility: visible;
    }

    .modal {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      width: 90%;
      max-width: 600px;
      max-height: 80vh;
      overflow-y: auto;
      transform: scale(0.95);
      transition: var(--transition);
    }

    .modal-overlay.visible .modal {
      transform: scale(1);
    }

    .modal-header {
      padding: 20px;
      border-bottom: 1px solid var(--border);
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .modal-title {
      font-size: 18px;
      font-weight: 600;
      color: var(--text-primary);
    }

    .modal-close {
      width: 32px;
      height: 32px;
      display: flex;
      align-items: center;
      justify-content: center;
      border-radius: var(--radius-sm);
      background: none;
      border: none;
      color: var(--text-muted);
      cursor: pointer;
      transition: var(--transition);
    }

    .modal-close:hover {
      color: var(--text-primary);
      background: var(--surface-light);
    }

    .modal-body {
      padding: 20px;
    }

    /* Statistiques détaillées */
    .detailed-stats {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
      gap: 16px;
      margin-bottom: 24px;
    }

    .detailed-stat {
      text-align: center;
      padding: 16px;
      background: var(--surface-light);
      border-radius: var(--radius-sm);
      border: 1px solid var(--border);
    }

    .detailed-stat-value {
      font-size: 20px;
      font-weight: 700;
      color: var(--text-primary);
      margin-bottom: 4px;
    }

    .detailed-stat-label {
      font-size: 11px;
      color: var(--text-secondary);
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    /* Chart placeholder */
    .chart-placeholder {
      height: 200px;
      background: var(--surface-light);
      border: 1px solid var(--border);
      border-radius: var(--radius-sm);
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--text-muted);
      font-size: 13px;
    }

    /* Responsive */
    @media (max-width: 1024px) {
      .smartlink-item {
        grid-template-columns: 60px 1fr auto;
        gap: 12px;
      }
      
      .smartlink-stats {
        display: none;
      }
    }

    @media (max-width: 768px) {
      .container {
        padding: 16px;
      }
      
      .page-header {
        flex-direction: column;
        gap: 16px;
        align-items: stretch;
      }
      
      .controls {
        flex-direction: column;
        gap: 12px;
        align-items: stretch;
      }
      
      .controls-left {
        flex-direction: column;
        align-items: stretch;
      }
      
      .search-input {
        width: 100%;
      }
      
      .smartlink-item {
        grid-template-columns: 48px 1fr auto;
        gap: 12px;
        padding: 12px;
      }
      
      .smartlink-actions {
        opacity: 1;
      }
      
      .stats-grid {
        grid-template-columns: repeat(2, 1fr);
      }
    }

    /* Animations */
    .fade-in {
      animation: fadeIn 0.3s ease-out;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(8px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .loading-spinner {
      width: 16px;
      height: 16px;
      border: 2px solid transparent;
      border-top: 2px solid currentColor;
      border-radius: 50%;
      animation: spin 1s linear infinite;
    }

    @keyframes spin {
      to { transform: rotate(360deg); }
    }

    /* Status badges */
    .status-badge {
      font-size: 10px;
      padding: 2px 6px;
      border-radius: 4px;
      font-weight: 500;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .status-badge.active {
      background: rgba(16, 185, 129, 0.2);
      color: var(--success);
    }

    .status-badge.draft {
      background: rgba(156, 163, 175, 0.2);
      color: #9ca3af;
    }

    .status-badge.paused {
      background: rgba(245, 158, 11, 0.2);
      color: var(--warning);
    }
  </style>
</head>

<body>
  <!-- Header avec navigation -->
  <header class="header">
    <div class="header-content">
      <div class="header-left">
        <img src="/images/MDMC_logo_blanc fond transparent.png" alt="MDMC" class="logo">
        <nav class="nav-tabs">
          <a href="/dashboard" class="nav-tab">Créer</a>
          <a href="/dashboard/manage" class="nav-tab active">Mes SmartLinks</a>
          <a href="/dashboard/analytics" class="nav-tab">Analytics</a>
        </nav>
      </div>
      <div class="header-right">
        <div class="user-info">Dashboard SmartLinks</div>
        <button class="logout-btn" id="logoutBtn">Déconnexion</button>
      </div>
    </div>
  </header>

  <div class="container">
    
    <!-- Header de page -->
    <div class="page-header">
      <div class="page-title-section">
        <h1>Mes SmartLinks</h1>
        <p class="page-subtitle">Gérez et analysez vos liens universels</p>
      </div>
      <div class="page-actions">
        <button class="btn btn-secondary">
          <span>📊</span> Exporter
        </button>
        <a href="/dashboard" class="btn btn-primary">
          <span>+</span> Nouveau SmartLink
        </a>
      </div>
    </div>

    <!-- Statistiques globales -->
    <div class="stats-grid">
      <div class="stat-card">
        <div class="stat-value">24</div>
        <div class="stat-label">SmartLinks actifs</div>
        <div class="stat-change positive">
          <span>↗</span> +3 ce mois
        </div>
      </div>
      <div class="stat-card">
        <div class="stat-value">12.4K</div>
        <div class="stat-label">Clics totaux</div>
        <div class="stat-change positive">
          <span>↗</span> +18% vs mois dernier
        </div>
      </div>
      <div class="stat-card">
        <div class="stat-value">8.7%</div>
        <div class="stat-label">Taux de conversion</div>
        <div class="stat-change negative">
          <span>↘</span> -0.3% vs mois dernier
        </div>
      </div>
      <div class="stat-card">
        <div class="stat-value">47</div>
        <div class="stat-label">Plateformes connectées</div>
        <div class="stat-change positive">
          <span>↗</span> Toutes disponibles
        </div>
      </div>
    </div>

    <!-- Contrôles et filtres -->
    <div class="controls">
      <div class="controls-left">
        <input 
          type="text" 
          class="search-input" 
          placeholder="Rechercher par titre, artiste ou URL..."
          id="searchInput"
        >
        <select class="filter-select" id="statusFilter">
          <option value="">Tous les statuts</option>
          <option value="active">Actifs</option>
          <option value="draft">Brouillons</option>
          <option value="paused">En pause</option>
        </select>
        <select class="filter-select" id="dateFilter">
          <option value="">Toutes les dates</option>
          <option value="today">Aujourd'hui</option>
          <option value="week">Cette semaine</option>
          <option value="month">Ce mois</option>
          <option value="year">Cette année</option>
        </select>
      </div>
      <div class="controls-right">
        <button class="btn btn-ghost btn-sm" id="refreshBtn">
          <span>🔄</span> Actualiser
        </button>
        <button class="btn btn-ghost btn-sm" id="viewToggle">
          <span>⊞</span> Vue
        </button>
      </div>
    </div>

    <!-- Liste des SmartLinks -->
    <div class="smartlinks-list" id="smartlinksList">
      
      <!-- SmartLink 1 -->
      <div class="smartlink-item fade-in">
        <img src="https://i.scdn.co/image/ab67616d0000b27349de1b4acdde02e84c6023b7" alt="Wait and Bleed" class="smartlink-artwork">
        <div class="smartlink-info">
          <div class="smartlink-title">Wait and Bleed</div>
          <div class="smartlink-artist">Slipknot</div>
          <div class="smartlink-meta">
            <span class="status-badge active">Actif</span>
            <span class="platform-count">8 plateformes</span>
            <span>Créé il y a 2 jours</span>
          </div>
        </div>
        <div class="smartlink-stats">
          <div class="stat-item">
            <span class="stat-number">1.2K</span>
            <span class="stat-text">Clics</span>
          </div>
          <div class="stat-item">
            <span class="stat-number">89</span>
            <span class="stat-text">Aujourd'hui</span>
          </div>
          <div class="stat-item">
            <span class="stat-number">12.3%</span>
            <span class="stat-text">CTR</span>
          </div>
        </div>
        <div class="smartlink-actions">
          <button class="action-btn" onclick="openStats('slipknot-wait-and-bleed')" title="Voir les statistiques">
            📊
          </button>
          <button class="action-btn primary" onclick="openLink('https://smartlink.mdmcmusicads.com/slipknot/wait-and-bleed')" title="Ouvrir le SmartLink">
            🔗
          </button>
          <button class="action-btn" onclick="editSmartLink('slipknot-wait-and-bleed')" title="Modifier">
            ✏️
          </button>
          <button class="action-btn danger" onclick="deleteSmartLink('slipknot-wait-and-bleed')" title="Supprimer">
            🗑️
          </button>
        </div>
      </div>

      <!-- SmartLink 2 -->
      <div class="smartlink-item fade-in">
        <img src="https://i.scdn.co/image/ab67616d0000b273b8b7f85671a0dd5ea957ec6d" alt="Master of Puppets" class="smartlink-artwork">
        <div class="smartlink-info">
          <div class="smartlink-title">Master of Puppets</div>
          <div class="smartlink-artist">Metallica</div>
          <div class="smartlink-meta">
            <span class="status-badge active">Actif</span>
            <span class="platform-count">10 plateformes</span>
            <span>Créé il y a 5 jours</span>
          </div>
        </div>
        <div class="smartlink-stats">
          <div class="stat-item">
            <span class="stat-number">2.8K</span>
            <span class="stat-text">Clics</span>
          </div>
          <div class="stat-item">
            <span class="stat-number">156</span>
            <span class="stat-text">Aujourd'hui</span>
          </div>
          <div class="stat-item">
            <span class="stat-number">18.7%</span>
            <span class="stat-text">CTR</span>
          </div>
        </div>
        <div class="smartlink-actions">
          <button class="action-btn" onclick="openStats('metallica-master-of-puppets')" title="Voir les statistiques">
            📊
          </button>
          <button class="action-btn primary" onclick="openLink('https://smartlink.mdmcmusicads.com/metallica/master-of-puppets')" title="Ouvrir le SmartLink">
            🔗
          </button>
          <button class="action-btn" onclick="editSmartLink('metallica-master-of-puppets')" title="Modifier">
            ✏️
          </button>
          <button class="action-btn danger" onclick="deleteSmartLink('metallica-master-of-puppets')" title="Supprimer">
            🗑️
          </button>
        </div>
      </div>

      <!-- SmartLink 3 -->
      <div class="smartlink-item fade-in">
        <img src="https://i.scdn.co/image/ab67616d0000b273d60776e0b24ad39e71fe8732" alt="One More Time" class="smartlink-artwork">
        <div class="smartlink-info">
          <div class="smartlink-title">One More Time</div>
          <div class="smartlink-artist">Daft Punk</div>
          <div class="smartlink-meta">
            <span class="status-badge draft">Brouillon</span>
            <span class="platform-count">6 plateformes</span>
            <span>Créé il y a 1 heure</span>
          </div>
        </div>
        <div class="smartlink-stats">
          <div class="stat-item">
            <span class="stat-number">-</span>
            <span class="stat-text">Clics</span>
          </div>
          <div class="stat-item">
            <span class="stat-number">-</span>
            <span class="stat-text">Aujourd'hui</span>
          </div>
          <div class="stat-item">
            <span class="stat-number">-</span>
            <span class="stat-text">CTR</span>
          </div>
        </div>
        <div class="smartlink-actions">
          <button class="action-btn" onclick="openStats('daft-punk-one-more-time')" title="Voir les statistiques">
            📊
          </button>
          <button class="action-btn primary" onclick="openLink('https://smartlink.mdmcmusicads.com/daft-punk/one-more-time')" title="Ouvrir le SmartLink">
            🔗
          </button>
          <button class="action-btn" onclick="editSmartLink('daft-punk-one-more-time')" title="Modifier">
            ✏️
          </button>
          <button class="action-btn danger" onclick="deleteSmartLink('daft-punk-one-more-time')" title="Supprimer">
            🗑️
          </button>
        </div>
      </div>

      <!-- SmartLink 4 -->
      <div class="smartlink-item fade-in">
        <img src="https://i.scdn.co/image/ab67616d0000b273e17c0aa1714a03d3b78a6978" alt="Bohemian Rhapsody" class="smartlink-artwork">
        <div class="smartlink-info">
          <div class="smartlink-title">Bohemian Rhapsody</div>
          <div class="smartlink-artist">Queen</div>
          <div class="smartlink-meta">
            <span class="status-badge paused">En pause</span>
            <span class="platform-count">9 plateformes</span>
            <span>Créé il y a 1 semaine</span>
          </div>
        </div>
        <div class="smartlink-stats">
          <div class="stat-item">
            <span class="stat-number">3.4K</span>
            <span class="stat-text">Clics</span>
          </div>
          <div class="stat-item">
            <span class="stat-number">12</span>
            <span class="stat-text">Aujourd'hui</span>
          </div>
          <div class="stat-item">
            <span class="stat-number">8.2%</span>
            <span class="stat-text">CTR</span>
          </div>
        </div>
        <div class="smartlink-actions">
          <button class="action-btn" onclick="openStats('queen-bohemian-rhapsody')" title="Voir les statistiques">
            📊
          </button>
          <button class="action-btn primary" onclick="openLink('https://smartlink.mdmcmusicads.com/queen/bohemian-rhapsody')" title="Ouvrir le SmartLink">
            🔗
          </button>
          <button class="action-btn" onclick="editSmartLink('queen-bohemian-rhapsody')" title="Modifier">
            ✏️
          </button>
          <button class="action-btn danger" onclick="deleteSmartLink('queen-bohemian-rhapsody')" title="Supprimer">
            🗑️
          </button>
        </div>
      </div>

    </div>

  </div>

  <!-- Modal des statistiques détaillées -->
  <div class="modal-overlay" id="statsModal">
    <div class="modal">
      <div class="modal-header">
        <h3 class="modal-title" id="statsModalTitle">Statistiques détaillées</h3>
        <button class="modal-close" onclick="closeStatsModal()">✕</button>
      </div>
      <div class="modal-body">
        
        <!-- Track info -->
        <div class="track-card" style="margin-bottom: 24px;">
          <img id="statsTrackArtwork" class="smartlink-artwork" src="" alt="">
          <div class="smartlink-info">
            <div class="smartlink-title" id="statsTrackTitle">-</div>
            <div class="smartlink-artist" id="statsTrackArtist">-</div>
            <div class="smartlink-meta">
              <span id="statsTrackUrl" style="font-family: 'JetBrains Mono', monospace; font-size: 10px;">-</span>
            </div>
          </div>
        </div>

        <!-- Statistiques détaillées -->
        <div class="detailed-stats">
          <div class="detailed-stat">
            <div class="detailed-stat-value" id="statsTotalClicks">-</div>
            <div class="detailed-stat-label">Clics totaux</div>
          </div>
          <div class="detailed-stat">
            <div class="detailed-stat-value" id="statsUniqueVisitors">-</div>
            <div class="detailed-stat-label">Visiteurs uniques</div>
          </div>
          <div class="detailed-stat">
            <div class="detailed-stat-value" id="statsConversionRate">-</div>
            <div class="detailed-stat-label">Taux de conversion</div>
          </div>
          <div class="detailed-stat">
            <div class="detailed-stat-value" id="statsTopPlatform">-</div>
            <div class="detailed-stat-label">Plateforme populaire</div>
          </div>
        </div>

        <!-- Graphique temporaire -->
        <div class="chart-placeholder">
          📈 Graphique des clics dans le temps<br>
          <small>(Intégration Chart.js à venir)</small>
        </div>

      </div>
    </div>
  </div>

  <script>
    // État de l'application
    const appState = {
      smartlinks: [
        {
          id: 'slipknot-wait-and-bleed',
          title: 'Wait and Bleed',
          artist: 'Slipknot',
          artwork: 'https://i.scdn.co/image/ab67616d0000b27349de1b4acdde02e84c6023b7',
          url: 'https://smartlink.mdmcmusicads.com/slipknot/wait-and-bleed',
          status: 'active',
          platforms: 8,
          createdAt: '2 jours',
          stats: {
            totalClicks: '1.2K',
            todayClicks: 89,
            conversionRate: '12.3%',
            uniqueVisitors: '987',
            topPlatform: 'Spotify'
          }
        },
        {
          id: 'metallica-master-of-puppets',
          title: 'Master of Puppets',
          artist: 'Metallica',
          artwork: 'https://i.scdn.co/image/ab67616d0000b273b8b7f85671a0dd5ea957ec6d',
          url: 'https://smartlink.mdmcmusicads.com/metallica/master-of-puppets',
          status: 'active',
          platforms: 10,
          createdAt: '5 jours',
          stats: {
            totalClicks: '2.8K',
            todayClicks: 156,
            conversionRate: '18.7%',
            uniqueVisitors: '2.1K',
            topPlatform: 'Apple Music'
          }
        },
        {
          id: 'daft-punk-one-more-time',
          title: 'One More Time',
          artist: 'Daft Punk',
          artwork: 'https://i.scdn.co/image/ab67616d0000b273d60776e0b24ad39e71fe8732',
          url: 'https://smartlink.mdmcmusicads.com/daft-punk/one-more-time',
          status: 'draft',
          platforms: 6,
          createdAt: '1 heure',
          stats: {
            totalClicks: '-',
            todayClicks: '-',
            conversionRate: '-',
            uniqueVisitors: '-',
            topPlatform: '-'
          }
        },
        {
          id: 'queen-bohemian-rhapsody',
          title: 'Bohemian Rhapsody',
          artist: 'Queen',
          artwork: 'https://i.scdn.co/image/ab67616d0000b273e17c0aa1714a03d3b78a6978',
          url: 'https://smartlink.mdmcmusicads.com/queen/bohemian-rhapsody',
          status: 'paused',
          platforms: 9,
          createdAt: '1 semaine',
          stats: {
            totalClicks: '3.4K',
            todayClicks: 12,
            conversionRate: '8.2%',
            uniqueVisitors: '2.8K',
            topPlatform: 'YouTube Music'
          }
        }
      ],
      filteredSmartlinks: [],
      searchTerm: '',
      statusFilter: '',
      dateFilter: ''
    };

    // Éléments du DOM
    const elements = {
      searchInput: document.getElementById('searchInput'),
      statusFilter: document.getElementById('statusFilter'),
      dateFilter: document.getElementById('dateFilter'),
      smartlinksList: document.getElementById('smartlinksList'),
      statsModal: document.getElementById('statsModal'),
      refreshBtn: document.getElementById('refreshBtn'),
      logoutBtn: document.getElementById('logoutBtn')
    };

    // Initialisation
    appState.filteredSmartlinks = [...appState.smartlinks];

    // Fonctions des actions
    window.openStats = function(smartlinkId) {
      const smartlink = appState.smartlinks.find(s => s.id === smartlinkId);
      if (!smartlink) return;

      // Remplir les données du modal
      document.getElementById('statsModalTitle').textContent = `Statistiques - ${smartlink.title}`;
      document.getElementById('statsTrackArtwork').src = smartlink.artwork;
      document.getElementById('statsTrackTitle').textContent = smartlink.title;
      document.getElementById('statsTrackArtist').textContent = smartlink.artist;
      document.getElementById('statsTrackUrl').textContent = smartlink.url;
      
      document.getElementById('statsTotalClicks').textContent = smartlink.stats.totalClicks;
      document.getElementById('statsUniqueVisitors').textContent = smartlink.stats.uniqueVisitors;
      document.getElementById('statsConversionRate').textContent = smartlink.stats.conversionRate;
      document.getElementById('statsTopPlatform').textContent = smartlink.stats.topPlatform;

      // Afficher le modal
      elements.statsModal.classList.add('visible');
    };

    window.closeStatsModal = function() {
      elements.statsModal.classList.remove('visible');
    };

    window.openLink = function(url) {
      window.open(url, '_blank');
    };

    window.editSmartLink = function(smartlinkId) {
      // Redirection vers l'éditeur avec l'ID
      window.location.href = `/dashboard/edit/${smartlinkId}`;
    };

    window.deleteSmartLink = function(smartlinkId) {
      const smartlink = appState.smartlinks.find(s => s.id === smartlinkId);
      if (!smartlink) return;

      if (confirm(`Êtes-vous sûr de vouloir supprimer le SmartLink "${smartlink.title}" de ${smartlink.artist} ?\n\nCette action est irréversible.`)) {
        // Simulation de suppression
        console.log(`Suppression du SmartLink: ${smartlinkId}`);
        
        // Retrait de la liste (simulation)
        const index = appState.smartlinks.findIndex(s => s.id === smartlinkId);
        if (index > -1) {
          appState.smartlinks.splice(index, 1);
          filterSmartLinks();
          renderSmartLinks();
        }
        
        // Toast de confirmation
        showToast('SmartLink supprimé avec succès', 'success');
      }
    };

    // Filtrage et recherche
    function filterSmartLinks() {
      let filtered = [...appState.smartlinks];

      // Filtre par terme de recherche
      if (appState.searchTerm) {
        const term = appState.searchTerm.toLowerCase();
        filtered = filtered.filter(s => 
          s.title.toLowerCase().includes(term) ||
          s.artist.toLowerCase().includes(term) ||
          s.url.toLowerCase().includes(term)
        );
      }

      // Filtre par statut
      if (appState.statusFilter) {
        filtered = filtered.filter(s => s.status === appState.statusFilter);
      }

      // Filtre par date (simulation)
      if (appState.dateFilter) {
        // Logique de filtrage par date à implémenter
      }

      appState.filteredSmartlinks = filtered;
    }

    function renderSmartLinks() {
      if (appState.filteredSmartlinks.length === 0) {
        elements.smartlinksList.innerHTML = `
          <div class="empty-state">
            <h3>Aucun SmartLink trouvé</h3>
            <p>Essayez de modifier vos filtres ou créez votre premier SmartLink</p>
            <a href="/dashboard" class="btn btn-primary">+ Créer un SmartLink</a>
          </div>
        `;
        return;
      }

      // Re-render de la liste (simulation - en vrai, on ferait un DOM update plus optimisé)
      console.log(`Affichage de ${appState.filteredSmartlinks.length} SmartLinks`);
    }

    // Event listeners
    elements.searchInput.addEventListener('input', (e) => {
      appState.searchTerm = e.target.value;
      filterSmartLinks();
      renderSmartLinks();
    });

    elements.statusFilter.addEventListener('change', (e) => {
      appState.statusFilter = e.target.value;
      filterSmartLinks();
      renderSmartLinks();
    });

    elements.dateFilter.addEventListener('change', (e) => {
      appState.dateFilter = e.target.value;
      filterSmartLinks();
      renderSmartLinks();
    });

    elements.refreshBtn.addEventListener('click', () => {
      // Simulation de refresh
      elements.refreshBtn.innerHTML = '<div class="loading-spinner"></div> Actualisation...';
      
      setTimeout(() => {
        elements.refreshBtn.innerHTML = '<span>🔄</span> Actualiser';
        showToast('Données actualisées', 'success');
      }, 1000);
    });

    // Fermeture du modal en cliquant sur l'overlay
    elements.statsModal.addEventListener('click', (e) => {
      if (e.target === elements.statsModal) {
        closeStatsModal();
      }
    });

    // Fermeture du modal avec Escape
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && elements.statsModal.classList.contains('visible')) {
        closeStatsModal();
      }
    });

    // Déconnexion
    elements.logoutBtn.addEventListener('click', () => {
      if (confirm('Voulez-vous vraiment vous déconnecter ?')) {
        window.location.href = '/';
      }
    });

    // Toast notifications (simple)
    function showToast(message, type = 'info') {
      // Implémentation simple d'un toast
      const toast = document.createElement('div');
      toast.style.cssText = `
        position: fixed;
        top: 20px;
        right: 20px;
        background: var(--surface);
        border: 1px solid var(--border);
        border-radius: var(--radius-sm);
        padding: 12px 16px;
        color: var(--text-primary);
        font-size: 13px;
        z-index: 10000;
        box-shadow: var(--shadow-lg);
        animation: slideIn 0.3s ease-out;
      `;
      
      if (type === 'success') {
        toast.style.borderColor = 'var(--success)';
        toast.style.background = 'rgba(16, 185, 129, 0.1)';
      }
      
      toast.textContent = message;
      document.body.appendChild(toast);
      
      setTimeout(() => {
        toast.style.animation = 'slideOut 0.3s ease-out forwards';
        setTimeout(() => document.body.removeChild(toast), 300);
      }, 3000);
    }

    // Animation CSS pour les toasts
    const style = document.createElement('style');
    style.textContent = `
      @keyframes slideIn {
        from { transform: translateX(100%); opacity: 0; }
        to { transform: translateX(0); opacity: 1; }
      }
      @keyframes slideOut {
        from { transform: translateX(0); opacity: 1; }
        to { transform: translateX(100%); opacity: 0; }
      }
    `;
    document.head.appendChild(style);

    console.log('Dashboard SmartLinks Management chargé avec succès');
  </script>
</body>
</html>